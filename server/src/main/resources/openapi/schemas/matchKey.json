{
  "description": "shared index match key",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description" : "match key identifier"
    },
    "matcher": {
      "type": "string",
      "description": "matcher module invocation: moduleId<::function> (the ::function suffix is optional)"
    },
    "args": {
      "type": "string",
      "enum": ["full", "payload"],
      "description": "how arguments are passed to matcher, full: full record, payload: only record payload"
    },
    "update": {
      "type": "string",
      "enum": ["ingest", "manual"],
      "description": "ingest: update when records are ingested, manual: update only when initialize is called"
    }
  },
  "additionalProperties": false,
  "required": [
    "id"
  ]
}
