# deprecated pool collection response
---
description: Pool configurations returned by the deprecated matchkeys endpoint
type: object
properties:
  matchKeys:
    description: Deprecated response property containing pool configurations
    type: array
    items:
      $ref: matchKey.yaml
  resultInfo:
    $ref: resultInfo.json
additionalProperties: false
required:
  - matchKeys
