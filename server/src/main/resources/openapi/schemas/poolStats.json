{
  "description": "Pool statistics",
  "type": "object",
  "properties": {
    "recordsTotal": {
      "type": "integer",
      "description": "Total records assigned to the pool"
    },
    "clustersTotal": {
      "description": "number of clusters",
      "type": "integer"
    },
    "matchValuesPerCluster": {
      "type": "object",
      "description": "key is number of match values in cluster; value is number of clusters"
    },
    "recordsPerCluster": {
      "type": "object",
      "description": "key is number of records in a cluster; value is number of clusters of this size (key)"
    },
    "recordsPerClusterSample": {
      "type": "object",
      "description": "key is number of records in a cluster; value is array of some cluster identifiers with this size"
    }
  },
  "required": [
    "recordsTotal"
  ],
  "additionalProperties": true
}
